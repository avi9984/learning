// ==(Abstract Equality operator): It checks the type of both operends, if type is same, then it calls ===, if types are not same then type conversion occurs(coercion) & then comparison is done.

//===(Strict equlity operator): It also checks type of both the operend, If type are differnet it returns false, If types are same then value comparison help

console.log(1=="1"); // true
console.log(1=="avi"); // NaN

