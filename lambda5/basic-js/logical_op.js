
// Logical operators

console.log(true && false);
console.log(false || false);
console.log(true && true);
console.log((5 < 10) && (6 < 3));

//In a AND gate, if the first input is false, then it does't evaluate the second input and immediately return the first input. as well if first input is true, then the scond input has to be evalulated and then return the second input.

console.log(6 && 10);
console.log(10 && 6);

// In a OR gate if the first input is true, then it does't evaluate the second input & immediately return first input as well as if first input is false, then the second input has to be evaluated and return the second input.




